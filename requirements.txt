annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
click==8.3.2
colorama==0.4.6
fastapi==0.135.3
greenlet==3.4.0
h11==0.16.0
idna==3.11
psycopg2-binary==2.9.11
pydantic==2.13.0
pydantic_core==2.46.0
python-dotenv==1.2.2
SQLAlchemy==2.0.49
starlette==1.0.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.44.0
# LLM runtime: llama-cpp-python + huggingface_hub instead of torch/transformers.
# Rationale: torch 2.11.0+cpu segfaults (exit 139) on Windows when loading
# Qwen2.5-1.5B through transformers.pipeline. llama-cpp-python ships prebuilt
# wheels, runs the same Qwen2.5-1.5B-Instruct via a GGUF build, and avoids
# the segfault entirely. Boss requirement met: every fuzzy field is still
# produced by the Qwen LLM, not by regex.
llama-cpp-python==0.3.20
huggingface_hub==1.10.2
requests
beautifulsoup4
lxml
python-multipart